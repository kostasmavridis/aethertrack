# Top-level 'version' key removed (obsolete in Compose v2)
services:

  # ── PostgreSQL 18 ───────────────────────────────────────────────────
  # BREAKING CHANGE in PG 18 (vs PG 17 and below):
  #   - PGDATA default changed from /var/lib/postgresql/data
  #     to /var/lib/postgresql/18/docker
  #   - The declared VOLUME also changed to /var/lib/postgresql (the parent).
  #   - Volume mounts must target /var/lib/postgresql, NOT /data.
  #   - PGDATA is explicitly set here for clarity and future-proofing.
  # Reference: https://hub.docker.com/_/postgres (PG 18 changelog)
  postgres:
    image: postgres:18-alpine
    container_name: aethertrack-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB:       aethertrack
      POSTGRES_USER:     aethertrack
      POSTGRES_PASSWORD: aethertrack_secret
      # Explicit PGDATA matching the PG 18 default; actual data lives at
      # <volume>/18/docker inside the named volume.
      PGDATA: /var/lib/postgresql/18/docker
    ports:
      - "5432:5432"
    volumes:
      # Mount the PARENT directory, not the old /data subfolder.
      # PG 18 writes to postgres_data:/var/lib/postgresql/18/docker
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aethertrack -d aethertrack"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # ── Kafka (Redpanda KRaft – no ZooKeeper) ────────────────────────────
  kafka:
    image: redpandadata/redpanda:v25.3.9
    container_name: aethertrack-kafka
    restart: unless-stopped
    command:
      - redpanda
      - start
      - --overprovisioned
      - --smp
      - "1"
      - --memory
      - 512M
      - --reserve-memory
      - 0M
      - --node-id
      - "0"
      - --check=false
      - --kafka-addr
      - PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr
      - PLAINTEXT://kafka:9092
    ports:
      - "9092:9092"
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster info --brokers localhost:9092 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 10s

  # ── HAPI FHIR R5 Server ────────────────────────────────────────────────
  hapi-fhir:
    image: hapiproject/hapi:latest
    container_name: aethertrack-fhir
    restart: unless-stopped
    environment:
      hapi.fhir.fhir_version: R5
      spring.datasource.url:      jdbc:postgresql://postgres:5432/aethertrack
      spring.datasource.username: aethertrack
      spring.datasource.password: aethertrack_secret
      spring.datasource.driverClassName: org.postgresql.Driver
      spring.jpa.properties.hibernate.dialect: ca.uhn.fhir.jpa.model.dialect.HapiFhirPostgres94Dialect
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/fhir/metadata > /dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 60s

  # ── Application services (uncommented slice by slice from Slice 18) ───
  # supplement-domain-service:
  #   build: ../backend/supplement-domain-service
  #   ports: ["8081:8081"]
  #   depends_on:
  #     postgres: { condition: service_healthy }
  #     kafka:    { condition: service_healthy }
  #
  # scheduling-service:
  #   build: ../backend/scheduling-service
  #   ports: ["8082:8082"]
  #
  # fhir-service:
  #   build: ../backend/fhir-service
  #   ports: ["8083:8083"]
  #
  # intake-service:
  #   build: ../backend/intake-service
  #   ports: ["8084:8084"]
  #
  # frontend:
  #   build: ../frontend
  #   ports: ["3000:80"]

volumes:
  postgres_data:
