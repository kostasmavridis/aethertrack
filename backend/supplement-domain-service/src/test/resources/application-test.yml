spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  flyway:
    enabled: true
logging:
  level:
    com.aethertrack: DEBUG
    org.flywaydb: INFO
    org.hibernate.SQL: DEBUG
